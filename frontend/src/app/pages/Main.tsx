import * as React from 'react';
import { Stack, StackItem } from '@patternfly/react-core';

const Main: React.FC = () => (
  <Stack>
    <StackItem>Hello World</StackItem>
  </Stack>
);

export default Main;
